---
---
<html lang="en">
<head>
    <title>500 — We messed up...</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        /* Base reset */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            position: relative;
            background: black;
            color: #ffffff;
        }

        /* Starfield animation background */
        .stars, .stars2, .stars3 {
            position: absolute;
            width: 100%;
            height: 100%;
            background: transparent url('https://raw.githubusercontent.com/JulianLaval/canvas-space-stars/master/images/stars.png') repeat;
            animation: moveStars 100s linear infinite;
        }

        .stars2 {
            background-size: 200px 200px;
            opacity: 0.5;
            animation: moveStars 200s linear infinite;
        }

        .stars3 {
            background-size: 400px 400px;
            opacity: 0.3;
            animation: moveStars 300s linear infinite;
        }

        @keyframes moveStars {
            from { background-position: 0 0; }
            to { background-position: -10000px 5000px; }
        }

        .glass-container {
            position: relative;
            z-index: 1;
            background: rgba(0, 0, 0, 0.5);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 40px;
            text-align: center;
            max-width: 600px;
            margin: auto;
        }

        h1 {
            font-size: 7rem;
            margin: 0;
            color: #00fff7;
            text-shadow: 0 0 20px #00fff7, 0 0 40px #00fff7;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { text-shadow: 0 0 10px #00fff7, 0 0 20px #00fff7; }
            50% { text-shadow: 0 0 30px #00fff7, 0 0 60px #00fff7; }
            100% { text-shadow: 0 0 10px #00fff7, 0 0 20px #00fff7; }
        }

        p {
            font-size: 1.2rem;
            margin: 20px 0;
        }

        a {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 24px;
            background: rgba(0, 255, 247, 0.2);
            color: #00fff7;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            border: 1px solid #00fff7;
            transition: all 0.3s ease;
            backdrop-filter: blur(5px);
        }

        a:hover {
            background: rgba(0, 255, 247, 0.4);
            color: #000000;
            border-color: #00fff7;
            transform: scale(1.05);
            box-shadow: 0 0 20px #00fff7, 0 0 40px #00fff7;
        }

        code {
            background: rgba(255, 255, 255, 0.1);
            padding: 2px 6px;
            border-radius: 4px;
        }

        /* Center container wrapper */
        .center-wrapper {
            position: relative;
            z-index: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100%;
            padding: 20px;
        }
    </style>
</head>
<body>
<div class="stars"></div>
<div class="stars2"></div>
<div class="stars3"></div>

<div class="center-wrapper">
    <div class="glass-container">
        <h1>500</h1>
        <p>
            Uh-oh! Something broke on our side.<br/>
            Maybe the server tripped over a cable or your mom found the power button again &#128064;.
        </p>
        <a href="/">Take me home</a>
    </div>
</div>
</body>
</html>
